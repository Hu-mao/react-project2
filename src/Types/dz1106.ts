export type Film = {
    name: string,
    director: string,
    ProdYear: number,
    fStudio: string
}

export type Profile = {
    fullName: string;
    phone: string;
    email: string;
    city: string;
};